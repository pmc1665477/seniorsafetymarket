# Generated by build/generate.js on every Netlify deploy — not committed, so the repo
# doesn't balloon to hundreds of tracked HTML files as listings grow (same pattern as
# helipadusa's .gitignore).
/listing/
/category/
/browse/
/sitemap.xml
/generator-status.txt

node_modules/

# Browser "Save Page As" artifacts — not source, not the real working file
index_files/
Senior Safety Market*.html
